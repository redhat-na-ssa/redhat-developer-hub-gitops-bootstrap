apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  name: gitlab-og
spec:
  targetNamespaces:
    - gitlab-system
  upgradeStrategy: Default
